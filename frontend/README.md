
  # Life Relevance Filter Prototype

  This is a code bundle for Life Relevance Filter Prototype. The original project is available at https://www.figma.com/design/2DoaTN8TxTA0rON52KD8lg/Life-Relevance-Filter-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
